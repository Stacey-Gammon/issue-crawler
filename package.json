{
  "name": "issue-crawler",
  "version": "0.1.0",
  "description": "Crawls GitHub issues and index them into an Elasticsearch instance",
  "main": "index.js",
  "scripts": {
    "test": "rimraf build && tsc && jest",
    "build": "rimraf build && tsc",
    "crawlRefs": "rimraf build && tsc && node build/api_reference_crawler/run.js",
    "crawlCode": "rimraf build && tsc && node build/code_crawler/run.js",
    "crawlIssues": "rimraf build && tsc && node build/issue_crawler/run.js"
  },
  "author": "Tim Roes <mail@timroes.de>",
  "license": "Apache-2.0",
  "dependencies": {
    "@octokit/plugin-retry": "^3.0.1",
    "@octokit/rest": "^18.0.6",
    "@types/elasticsearch": "^5.0.36",
    "elasticsearch": "^16.7.1",
    "find": "^0.3.0",
    "moment": "^2.24.0",
    "simple-git": "^2.21.0",
    "sloc": "^0.2.1",
    "tmp": "^0.2.1"
  },
  "devDependencies": {
    "@octokit/types": "^5.5.0",
    "@types/find": "^0.2.1",
    "@types/jest": "^26.0.14",
    "@types/sloc": "^0.2.0",
    "@types/tmp": "^0.2.0",
    "jest": "^26.4.2",
    "ts-morph": "^8.1.2",
    "typescript": "^4.0.2"
  }
}
